- [x] Define `ReloadStatus { ok: bool, policy_count: usize, last_error: Option<String>, last_reload_at: Option<chrono::DateTime<chrono::Utc>> }` and `AdminEvent` enum (`PolicyReloadOk { policy_count: usize }`, `PolicyReloadError { skipped_count: usize, db_error: Option<String> }`) in `admin/mod.rs`; derive `Debug, Clone, serde::Serialize`; add `last_reload_status: Arc<std::sync::Mutex<ReloadStatus>>` to `AuthzEngine`; update `reload_from_records_lenient` and `reload_from_database` to write to this field after every reload attempt
- [x] Add `admin_events: tokio::sync::broadcast::Sender<AdminEvent>` to `AdminState`; create the channel in `main.rs` (`broadcast::channel(256)`) and thread the `Sender` into `AdminState` and the `Receiver` side into the cache/NOTIFY reload path so the reload path can emit `AdminEvent` after every reload
- [x] Update `cache/mod.rs` NOTIFY-triggered reload path: after `authz.reload_from_database(db)`, send `AdminEvent::PolicyReloadOk` or `AdminEvent::PolicyReloadError` to the broadcast channel (obtain sender from a new `Arc<broadcast::Sender<AdminEvent>>` threaded through `GateCacheOptions` or passed directly to `start_cache_invalidation_listener`)
- [x] Add `GET /events` SSE route to `admin_router_with_auth`: subscribe to `admin_events` broadcast, stream events as `data: <json>\n\n` using `axum::response::Sse`; add `GET /policies/reload-status` route returning `ReloadStatus` read from `authz_engine.last_reload_status`
- [x] Add `require_policies_at_startup: bool` (default `false`, `#[serde(default)]`) to `ServerConfig` in `config/types.rs`; in `main.rs`, after building the `AuthzEngine`, if `require_policies_at_startup` is `true` and the engine's policy count is zero (or the build returned an error), log a fatal error and `std::process::exit(1)`; document the flag in `config.example.yaml`; tests: `reload_error_emits_admin_event`, `reload_ok_emits_admin_event`, `reload_status_endpoint_reflects_last_reload`, `startup_exits_when_require_policies_true_and_no_policies`; `cargo check/clippy -D warnings/test --workspace` green
