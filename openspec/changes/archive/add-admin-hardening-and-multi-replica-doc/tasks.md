- [x] `admin/mod.rs`: apply `axum::extract::DefaultBodyLimit::max(64 * 1024)` to the protected sub-router (between the route declarations and the auth `.layer()`); add test `admin_body_over_64k_returns_413` and `admin_body_at_limit_passes`
- [x] `approval/mod.rs` or `admin/mod.rs` tests: add `cross_replica_decision_returns_not_found` — create two independent `ApprovalManager` instances, register in one, decide in the other, assert `Err(ApprovalError::NotFound)` (proves the in-memory per-replica constraint is machine-verifiable)
- [x] `main.rs`: emit a structured startup `warn!()` when `admin_listen` is non-loopback AND `REPLICA_COUNT` env var parses to > 1 (or when `admin_listen` is non-loopback unconditionally as a softer hint); message: approval store is per-replica, configure sticky sessions or service mesh
- [x] `main.rs`: emit a startup `warn!()` when `server.admin_listen` is non-loopback and no CORS config is present (field `server.admin_cors` does not exist yet — emit the warning based on the non-loopback bind alone; the config field is future work)
- [x] `README.md`: add multi-replica deployment note to `### Pending Approvals` section (sticky session requirement, env var `REPLICA_COUNT` hint, link to the future Option 3a/3b roadmap); `cargo check/clippy/test --workspace` green; SEPARATED security review (startup warnings, body-limit, cross-replica test)
