- [x] Add `ApprovalError::CapExceeded` variant; add `max_pending: Option<usize>` and `janitor_interval_seconds: Option<u64>` to `ApprovalConfig` in `config/types.rs` (serde defaults: `max_pending` → `Some(1000)` via a `default_approval_max_pending` fn, `janitor_interval_seconds` → `None`)
- [x] `ApprovalManager::new()` accepts (or reads from a stored field) the cap; `register()` checks `inner.len() >= cap` before insert and returns `Err(ApprovalError::CapExceeded)` on breach; thread the cap from `ApprovalConfig` through `main.rs` → `ApprovalManager`
- [x] `middleware/pipeline.rs`: in the `request_approval()` call's error branch, treat `ApprovalError::CapExceeded` as fail-closed Deny (same path as any other registration error — emit deny event, resume stream to termination, log a WARN)
- [x] Tests: `register_at_cap_returns_cap_exceeded`; `second_register_when_under_cap_succeeds`; `pipeline_cap_exceeded_denies_not_panics` (processor-level test); `janitor_interval_config_is_used_when_set` (unit test on the derived interval value)
- [x] `main.rs` reads `approval.janitor_interval_seconds` and uses it directly (no heuristic) when `Some`; falls back to `ttl/2, min 10, else 60` when `None`; `config.example.yaml` documents `max_pending` and `janitor_interval_seconds`; `cargo check/clippy/test --workspace` green
