name: flint_gate
description: Flutter/Dart client SDK for the Flint Gate AI auth proxy.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  http: ^1.2.0

dev_dependencies:
  flutter_lints: ^4.0.0
  test: ^1.25.0
