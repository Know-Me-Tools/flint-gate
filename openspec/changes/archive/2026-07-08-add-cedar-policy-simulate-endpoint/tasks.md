- [x] Define `SimulateRequest { principal: String, action: String, resource: String, context: serde_json::Value }` and `SimulateResponse { decision: String, reasons: Vec<String>, errors: Vec<String> }` in `admin/mod.rs`; derive `Debug, serde::Deserialize` / `Debug, serde::Serialize` respectively; document the expected Cedar EntityUid string format (`"Namespace::Type::\"id\""`) in the struct doc comments
- [x] Implement `POST /policies/simulate` handler: parse `principal`, `action`, `resource` as `cedar_policy::EntityUid` (return HTTP 422 with a descriptive `{ error, field, message }` body if any parse fails); construct a `cedar_policy::Context` from `context` (empty context if `null`); construct `cedar_policy::Entities::empty()`; call `authz_engine.is_authorized(request, &entities)`; map the `cedar_policy::Decision` to `"Allow"` / `"Deny"`; collect `response.diagnostics().reason()` policy IDs as `reasons` and `response.diagnostics().errors()` as `errors`; return `SimulateResponse` with HTTP 200
- [x] Add `POST /policies/simulate` route to `admin_router_with_auth` (rate-limited by existing `AdminGovernorLayer`; auth-required same as all other protected routes)
- [x] Tests in `admin/mod.rs`: `simulate_returns_allow_when_policy_permits`, `simulate_returns_deny_when_no_matching_allow`, `simulate_returns_422_for_invalid_entity_uid`, `simulate_includes_matching_policy_id_in_reasons`; `cargo check/clippy -D warnings/test --workspace` green
