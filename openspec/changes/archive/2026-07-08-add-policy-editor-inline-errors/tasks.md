- [x] Add `PolicyParseError` and `ValidateResponse { valid: boolean, errors: PolicyParseError[] }` TypeScript types to `web/src/types/` (or `web/src/api/admin.ts`); add `validatePolicy(text: string, schema?: string): Promise<ValidateResponse>` API function to `web/src/api/admin.ts` that calls `POST /policies/validate`
- [x] Add debounced validate-on-change to `PolicyForm` in `web/src/pages/Policies.tsx`: use `useRef` for the timeout handle; on Cedar textarea change, clear the timer, set a new 500ms timer that calls `validatePolicy`; store the `ValidateResponse` in local state; disable the Save button when `valid === false`; show a "Policy is valid" indicator when `valid === true` and the textarea is non-empty
- [x] Render the `errors[]` array from `ValidateResponse` as an inline error list directly below the Cedar textarea: each entry as `Line {line}, Col {column}: {message}` in a styled `<ul>` (red text, small font); clear the list when the textarea is empty or a new validation is in flight
- [x] Add a standalone "Validate" button (separate from Save) that immediately calls `validatePolicy` without debounce and shows the result in the same inline error list; add a loading state (spinner or disabled state) while validation is in flight
- [x] Wire admin SSE hot-reload events: in the Policies page, open an `EventSource` to `/admin/events`; on `PolicyReloadError` events, display a dismissable warning banner ("Policy reload error — N rows skipped. Check server logs."); on `PolicyReloadOk`, dismiss any existing banner; clean up the `EventSource` on component unmount; `tsc --noEmit` clean; `web` build green (`pnpm build` or equivalent)
