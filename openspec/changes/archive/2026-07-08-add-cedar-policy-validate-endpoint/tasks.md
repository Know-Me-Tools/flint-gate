- [x] Define `PolicyParseError { line: usize, column: usize, length: usize, message: String }` in `crates/flint-gate-core/src/authz/error.rs`; derive `Debug, Clone, serde::Serialize, serde::Deserialize`; add a constructor `fn from_cedar(src: &str, loc: Option<cedar_policy::SourceLocation>, msg: String) -> Self` that converts byte offset to 1-based line/column by scanning the source string
- [x] In `bundle.rs` and `validator.rs`, replace all `e.to_string()` / `.map(|e| e.to_string())` call sites with iteration over `ParseErrors` / `ValidationError`s that produce `Vec<PolicyParseError>` using the new constructor; update `AuthzError::PolicyParse` to carry `Vec<PolicyParseError>` and `AuthzError::ValidationErrors` to carry `Vec<PolicyParseError>`
- [x] Extract `pub fn validate_policy_text(text: &str, schema: Option<&cedar_policy::Schema>) -> Result<(), Vec<PolicyParseError>>` from `upsert_policy_inner` in `admin/mod.rs`; refactor `upsert_policy_inner` to call this shared function; update HTTP 422 response body to serialize `Vec<PolicyParseError>` as `errors` field
- [x] Add `POST /policies/validate` route to `admin_router_with_auth` and implement handler: deserialize `{ policy: String, schema: Option<String> }`, call `validate_policy_text`, return `{ valid: bool, errors: Vec<PolicyParseError> }` with HTTP 200 regardless of valid/invalid (the result is always a successful response)
- [x] Tests in `admin/mod.rs`: `validate_endpoint_returns_valid_true_for_good_policy`, `validate_endpoint_returns_errors_with_line_col_for_bad_syntax`, `validate_endpoint_returns_schema_validation_errors`, `upsert_policy_still_rejects_invalid_with_422`; `cargo check/clippy -D warnings/test --workspace` green
